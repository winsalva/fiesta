<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= assigns[:page_title] || "Fiesta · Phoenix Framework" %></title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <%= csrf_meta_tag() %>
  </head>
  <body class="flex flex-col md:flex-none bg-gray-100 min-h-screen"
    :class="{ 'overflow-x-hidden overflow-y-hidden': isDialogOpen }"
    x-data="{ 'isDialogOpen': false }"
    @keydown.escape="isDialogOpen = false">

    <%= render_existing @view_module, "header.html", conn: @conn %>

    <main role="main" class="flex-grow">
      <%= @inner_content %>
    </main>

    <footer class="bg-gray-900 text-white p-6 pb-0 flex flex-col divide-y-2 divide-white">
      <div class="text-2xl p-4">Fiesta</div>
      <div class="flex flex-col md:flex-row p-4 md:w-full">
        <div class="flex space-x-2 mb-4 md:w-1/3">
          <i data-feather="facebook"></i>
          <i data-feather="twitter"></i>
          <i data-feather="youtube"></i>
        </div>
        <div class="flex flex-col space-y-4 mb-4 md:w-1/3">
          <div>Terms of Restaurant</div>
          <div>Restaurant Privacy</div>
          <div>Consent Disagree</div>
          <div>Help</div>
        </div>
        <div class="flex flex-col space-y-4 mb-4 md:w-1/3">
          <div>About Us</div>
          <div>Trust & Safety</div>
        </div>
      </div>
      <div class="flex justify-center p-4 text-sm">
        &copy; 2021 SoftWLabs Inc.
      </div>
    </footer>

    <%= render CommonView, "flash_message.html", conn: @conn, class: "fixed p-6 md:p-0 md:top-8 md:right-1/2 z-50 md:transform md:translate-x-1/2" %>

    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
